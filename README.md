# flask-postgres-blog
A simple, CRUD (Create, Read, Update, Delete) blog application built using Flask and connected to a remote PostgreSQL database (via Supabase). Features include basic CRUD operations and a restricted admin delete key for security.
